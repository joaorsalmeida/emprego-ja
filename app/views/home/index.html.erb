
  <div class="col-md-9">
  <section>
        
        <h1>Últimas Vagas</h1>
        <% @job_positions.each do |vaga| %>
        <div class="row">
          <div id="vagas" class="col-md-12">
             <div id="logos" class="col-md-4 img-rounded">
                <%= image_tag vaga.company.avatar.url(), class:"img-rounded col-md-12"  %>
              </div>
            <div id="descricao" class="row col-md-4">
              <h2><%= vaga.job_category.description %></h2>
              <p><h3><%= vaga.created_at %></h3></p>
              <p><%= vaga.description %></p>
              <button class="btn btn-primary"  type="button"><%= link_to "Saiba mais", company_job_position_path(vaga.company, vaga, )%></button>
            </div>
          </div>
        </div>  
        <% end %>    
       
  </section>
  </div>
  <div class="col-md-3">
  <section id="filters">
   
    <div  class="row">
      <div class="col-md-12">
        <h2>Filtros:</h2>
        <strong>Categoria:</strong>
        <% JobCategory.job_category_filter.each do |c| %>
        <p><%= link_to c.description, c %></p>
        <% end %>  
      </div>
    </div>
   
  </section>  
  </div>